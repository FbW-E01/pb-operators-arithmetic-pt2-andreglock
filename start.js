const tickets = 6450 / 15;

console.log('Q1:', tickets, 'tickets were sold.');

const yearWage = 500 * 52.143;

console.log('Q2: Sylvia makes', yearWage, 'dollars per year.');

const percentage = (17 / 30) * 100;

console.log('Q3: %d %', percentage.toFixed(2));

const perimeterSquare = 4.75 * 4;

console.log('Q4: the perimeter of the square is %d cm', perimeterSquare);

const perimeterTriangle = 5 + 6 + 7;

console.log('Q5: the perimeter of the triangle is %d cm', perimeterTriangle);

const areaSquare = 5 * 5;

console.log('Q6: the area of the square is %d cm²', areaSquare);

const areaTriangle = 1 / 4 * Math.sqrt((5 + 6 + 7)*(- 5 + 6 + 7)*(5 - 6 + 7)*(5 + 6 - 7));

console.log('Q7: the area of the triangle is %d cm²', areaTriangle.toFixed(2));

const volumeCube = Math.pow(9, 3);

console.log('Q8: the volume of the cube is %s cm³', volumeCube);

const bills = [22.35, 26.67, 35.92];

console.log('Q9:  Bill 1: €%s\n     Bill 2: €%s\n     Bill 3: €%s', (bills[0] * 1.1).toFixed(2), (bills[1] * 1.15).toFixed(2), (bills[2] * 1.2).toFixed(2));

const hoursAvrg = (8 + 6 + 5 + 9 + 8 + 2 + 1 + 8.5 + 7 + 4)/ 10;

console.log("Q10: Noemy's averaged %s hours a day in the last two weeks", hoursAvrg);

const x = 6 * 85 - (75 + 70 + 85 + 90 + 100)

console.log('Q11: Score in the test: %s', x);

const y = 80 * 9 - 78 * 8;

console.log('Q12: James needs a minimum of %s%', y);

